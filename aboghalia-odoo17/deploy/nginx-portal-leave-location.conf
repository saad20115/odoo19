# DISABLED for beta — do NOT enable while testing on :8443.
#
# Users currently use: https://aboghaliaoffice.com:8443/
# Re-enable under portal only when moving to live (after UAT sign-off).
#
# Leaves PWA under portal HTTPS (no custom port)
# Paste inside the portal.aboghaliaoffice.com listen 443 server block
# BEFORE `location /`, then: nginx -t && systemctl reload nginx
#
# Live target later: https://portal.aboghaliaoffice.com/leave

# location /leave {
#     proxy_pass http://127.0.0.1:8095;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;
#     proxy_set_header X-Forwarded-Host $host;
#     proxy_set_header access-token $http_access_token;
#     proxy_set_header x-company-id $http_x_company_id;
#     proxy_set_header x-company-ids $http_x_company_ids;
#     proxy_set_header Cookie "";
#     proxy_hide_header Set-Cookie;
#     proxy_read_timeout 120s;
# }
#
# location = /leave/sw.js {
#     proxy_pass http://127.0.0.1:8095;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Proto https;
#     proxy_set_header Cookie "";
#     proxy_hide_header Set-Cookie;
#     add_header Service-Worker-Allowed /leave;
# }
