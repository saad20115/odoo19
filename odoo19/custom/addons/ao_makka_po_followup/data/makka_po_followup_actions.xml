<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_server_assign_to_accounting" model="ir.actions.server">
        <field name="name">Assign to Accounting</field>
        <field name="model_id" ref="model_makka_po_followup"/>
        <field name="binding_model_id" ref="model_makka_po_followup"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_assign_to_accounting()
        </field>
    </record>

    <record id="action_server_po_order_assign_user" model="ir.actions.server">
        <field name="name">Assign User</field>
        <field name="model_id" ref="model_makka_po_order"/>
        <field name="binding_model_id" ref="model_makka_po_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
action = records.action_open_assign_wizard()
        </field>
    </record>
</odoo>
